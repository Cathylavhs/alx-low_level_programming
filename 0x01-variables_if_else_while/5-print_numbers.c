#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
/*printing the numbers in base 10.*/
int number = 0;
for
(number  = 0;
number < 10;
number++)
{
printf("%d", number);
};
printf("\n");
return (0);
}
